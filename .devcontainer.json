{
  "image": "mcr.microsoft.com/devcontainers/base:debian-13",
  "postCreateCommand": "sudo apt update && sudo apt install just git-lfs",
  "postAttachCommand": "just -l && echo 'Run `just <recipe>` to start the given recipe.'"
}